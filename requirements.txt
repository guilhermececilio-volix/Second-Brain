# Sprint 1
google-genai
python-dotenv

# Sprint 2 — RAG (banco pgvector)
psycopg[binary]   # driver PostgreSQL (v3)
pgvector          # adaptador do tipo vector para psycopg

# Sprint 2 — interface web (Streamlit, versão inicial)
streamlit

# Sprint 3 — API para o frontend Next.js
fastapi
uvicorn[standard]
python-multipart   # upload de arquivos (.docx) na API

# Reunião → notas: ler transcrição de arquivos .docx (Word/Teams)
python-docx

# Integração com Google Calendar (OAuth + API)
google-auth
google-auth-oauthlib
google-api-python-client
